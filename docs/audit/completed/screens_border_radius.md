# Screens - Border Radius Audit

> `lib/screens/` BorderRadius.circular issues

## Summary
- **Total remaining**: 0 (all migrated to `AppTokens.radius.*`)

---

## Status
- All previously flagged `BorderRadius.circular` instances in `lib/screens/` (settings_screen, schedules_preview_sheet, reminders_cards, admin_issue_reports_page, add_reminder_page, add_class_page, about_sheet) now use `AppTokens.radius.*`.
- Fresh search shows no remaining `BorderRadius.circular` in `lib/screens/`.
